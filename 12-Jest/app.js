module.exports = {
  soma: function (a, b) {
    return a + b;
  },

  // Teste aponta Falha
  // mult: function (a, b) {
  //   return a - b;
  // }

  //// Teste aponta Acerto
  mult: function (a, b) {
    return a * b;
  }
}